import temp from './stosis.htm';

let $$;

export default function init() {
  __$routerOutlet.html(temp);
  
  $$ = __els('[data-root="stosis"]');
  
  /* $$.showUs.on('click', function () {
    $$.root.append( $(article) );
  }); */
}